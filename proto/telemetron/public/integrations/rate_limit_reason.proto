syntax = "proto3";

package SpaceX.API.Telemetron.Public.Integrations;

option go_package = "spacex.com/api/telemetron/public/integrations/ratelimitreason";

enum RateLimitReason {
  UNKNOWN = 0;

  NO_LIMIT = 1;

  POLICY_LIMIT = 2;

  USER_CUSTOM_LIMIT = 3;

  OVERAGE_LIMIT = 5;

  reserved 4;

  reserved "HIGH_HOURLY_AVG_LIMIT";
}
